[project]
name = "carevl-shared"
version = "2.0.0"
description = "Shared utilities for CareVL Edge and Hub"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=44.0.0",
    "pydantic>=2.10.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
